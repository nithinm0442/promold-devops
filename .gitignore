# Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfvars

# Environment files
.env

# OS / Editor junk
.DS_Store
Thumbs.db
.vscode/
.idea/
# Terraform bootstrap (local state only, never remote)
Terraform-Bootstrap/terraform.tfstate
Terraform-Bootstrap/terraform.tfstate.backup
Terraform-Bootstrap/.terraform/